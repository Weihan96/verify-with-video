---
name: verify-with-video
description: 通过真实界面操作、同步录屏和精简视频验证软件结果。用于录屏验收、修复后视频回归，或剪辑、配音已有验收录像；支持单任务及多任务的受限 Blender 后台操作与录屏；默认剪去等待、操作 2 倍速，并添加简短中文旁白。
---

# 录屏验收

让用户用一段简短、真实、看得清的视频判断结果。视频承担操作证据，当前对话承担简短结论；除非用户明确要求，不创建独立验收指南或重复操作清单。

## 确定本轮要证明什么

- 从当前请求提取验收范围、实际入口、预期状态与结果。已有明确范围时直接执行；不要把一次功能验收扩大成全产品审计。
- 用户只要求剪辑或配音已有验收录像时，直接检查素材并进入后期，不重新操作应用或修改代码。素材缺少结果证据时如实指出，不能用剪辑或旁白补造一次通过。
- 区分自动化测试通过、真实界面验证通过和用户确认验收。后台脚本、截图或生成成功都不能单独替代界面操作证据。
- 修复已获授权时，按“复现 → 修复 → 针对性回归 → 真实界面录屏复验”推进；复验发现同范围问题则继续修复。所有约定验收项都有通过证据后结束；遇到实际权限、环境或范围阻塞时明确报告未完成项，不能把“未能验证”写成通过。
- 本技能不新增代码修改、文件覆盖或外部发布权限；沿用当前会话已有授权，不重复索要同一授权。

## 选择操作方式

- Blender 验收先按所需操作选择输入方式。支持的后台操作默认走 [Blender 后台接入](references/blender-background.md) 的 `scripts/blender_background.py` 单任务入口：当前真实任务独立拥有临时实例、全屏桌面、输入与录屏，无需另建协调或参与任务。已有多任务并行安排时继续使用原多任务入口。普通业务保留场景，测试方块和控件只在显式 `--fixture` 时创建。
- 先确认场景启动来源：`.blend` 使用 `--blend`；IFC/Bonsai 使用 `--ifc`、实际 `--worktree`，需要项目插件时复用项目已有 `--prepare-script`，由已安装的 Bonsai 启动器完成加载与归属校验，再接入后台。不要因为现有窗口尚未接入或项目需要初始化脚本就直接退回前台；先检查能否用获授权的文件副本和初始化脚本建立本任务临时实例。未保存工作不能靠磁盘副本替代，不能接管现有窗口或丢弃草稿。
- 并行权限只覆盖组内已经核对的窗口和允许的模拟事件。准备步骤短暂排队，正式准入后立即释放队列；后台输入、录屏与局部清理独立继续。验收需要另存为、打开文件、独立弹窗或未验证快捷键时，起初就选择普通串行流程。并行实例没有自动切换成原生输入的入口，不能承诺中途无损切换。组外任务继续遵守原队列；不能删除队列或假借另一个 task 的身份。
- 其他应用及未接入的 Blender 实例继续使用下述原生桌面流程。仅按窗口后台录屏不等于后台输入已隔离。先确认实际输入模式再处理前台变化：原生输入失去目标焦点时停止输入；已正式准入的 Blender 后台输入和窗口录屏不因离开前台而停止，继续检查归属、窗口和录屏健康。不要仅因后台运行要求用户回复“现在录”。

## 共享桌面排队与交接

- 首次访问应用、输入、取证或录屏探针前，阅读 [桌面队列](references/desktop-queue.md)，普通任务或并行组协调者使用 `scripts/desktop_queue.py` 申请；已获授权的组内后台操作使用自己的 participant 凭据，不另外 request 或释放协调者租约；协调者完成正式准入后即释放准备租约并交接。组外任务仍正常排队。只剪辑或后台准备无需取号。
- 共用原有状态目录、号码、凭据、历史和通知。队列只是工具无关的协作状态，不是操作系统锁；绑定 PID 也不能让共享鼠标、焦点和剪贴板安全并行。不得插队、跳号、重置状态、结束他人占用或另建队列。
- 下述持续检查租约、结束后释放规则适用于普通前台流程及并行组准备阶段；已准入的 Blender 后台流程改查固定归属与录屏状态，收尾不再释放旧租约。
- 等待时继续不占桌面的准备；通知后重新申请并检查自己凭据。恢复上下文和每段操作前再次检查。实际桌面仍被他人使用时停止输入，不能用队列凭据抢占。
- 结束时停止本次前台租约保护的输入与录屏、保存证据，核对归属后关闭自己启动的临时应用，再释放自己的队列凭据。用户或其他任务窗口保留。无法确认输入/录制已停止时保留占用并报告；取消等待只撤自己的号。
- `release/cancel` 返回 `notify` 时必须用 `mcp__codex_app__send_message_to_thread` 发送指定通知，成功才登记 `notified`；失败保留待通知。此授权仅限队列交接，不扩大消息权限。旧脚本名继续兼容同一队列，日号机制不在本流程变更范围内。

## 真实操作与录制

- **本流程禁止调用内置 Computer Use（包括其截图、菜单或输入接口）**。macOS 使用本技能 [原生桌面工具](references/native-desktop.md)，先读该引用；不要加载官方 computer-use 插件作为本流程依赖，也不修改它。其他平台须使用已获授权、可验证归属的原生工具；没有时报告环境阻塞。
- 启动特定应用、模型或测试场景沿用领域启动流程；原生鼠标/键盘或上述已接入的 Blender 窗口模拟事件完成用户操作；后台直接赋值只能辅助准备或核对数据，不能冒充界面操作。
- 操作前核实本任务的应用实例和窗口。多实例同名时核对 PID、窗口或实际文件内容，不能仅凭应用名称猜测。验证录制画面对应同一目标，优先录目标窗口，避免混入其他任务和桌面通知。
- 新进程未出现在可见窗口列表时，按原始启动记录做进程限定查询，明确绑定窗口后恢复并核验可见/焦点；无法绑定时使用真实启动凭据清理，不能伪造 session 或转向另一同名实例。
- 同一工具至少跨两次独立调用验证可逆点击、单键、组合键及随后不带修饰键的输入；对比前后状态，不能凭第一次成功宣布稳定。工具 `sent` 仅是事件发送，必须查看界面实际变化。
- 先做短录制探针，确认有画面、尺寸正确、鼠标可见，弹窗和文件选择器也能被录到。若另一个窗口承载重要步骤，先调整捕获范围再继续。
- 短探针不能证明持续录制稳定。出现延迟或间歇故障时，回归原失败捕获模式，连续运行超过已知失败时间并覆盖间歇操作、截图、弹窗及空闲；已知分钟级故障须做数分钟回归。保留全长原片和监测日志，精剪成片时长不等于压力测试时长。失败原因不明时如实保留不确定性，不能仅放宽判定或凭一次短片宣布可用。
- 将环境准备与正式验收分开。正式片段保留操作前状态、用户操作、状态变化和结果；保存类操作还应展示重新打开或等价的持久化验证。
- **Blender/Bonsai 的三维验收画面必须使用透视（Perspective），不得用正交（Orthographic）替代。** 正式录制前核实实际被录视口的投影模式；若使用相机视图，核实该相机为透视相机。切换视图、重开文件或换窗口后重新确认。准备阶段使用正交检查时，须恢复透视后再录制正式操作与结果；不能以便于 agent 检查为由保留正交片段。
- Blender 模拟输入需要可辨认的光标时，读取 [Blender 输入光标标记](references/blender-cursor.md)，使用可选的窗口绑定标记；首次出现时说明它是辅助标注。标记不代表控件响应，也不提供多任务并行权限。
- 同步记录操作和关键结果的时间点，并对齐视频实际首帧。操作日志只能辅助找剪点：异步结果可能在最后一次点击之后出现，必须查看录屏确认。
- 保留原录屏与简洁的剪辑时间映射，便于回查。不要把项目名、用户目录、PID、窗口 ID 或时间戳固定写成可复用默认值。

## 剪成简短而可信的视频

用户偏好是**先精剪，再加速**。默认将普通操作片段渲染为 **2 倍速**，而不是只建议用户在播放器中调速。

- 代理思考、工具调用间隔、鼠标空转等操作停顿直接剪除，不计入应用耗时。应用自身的加载、计算等待也剪除；保留触发操作、必要的等待状态与最终结果，使“操作 → 变化 → 结果”链路可核对。
- 对明显较长、会影响观众判断响应速度的应用等待，在对应操作／结果片段**同时用清楚可读的字幕和旁白**披露本次耗时及已剪除等待，例如“本次应用计算约 40 秒，等待已剪除”。字幕留足阅读时间，放在不遮挡关键结果的位置；不能仅写泛泛的“已剪等待”。
- 耗时依据原始录像或可信时间记录，核对应用开始等待与结果可用的边界，不能把代理迟迟未检查结果的间隔算作应用等待，也不能从成片长度推算。在原片及剪辑时间映射中保留等待起止、测量依据和对应成片位置；边界不精确时披露有依据的估计范围，例如“本次加载估计 35–45 秒，等待已剪除”，不编造精确值。
- 不按固定时长硬截，也不把所有静止画面都删掉。静止的成功结果、错误提示和数值对照通常正是证据。
- 关键数值、结果和报错恢复原速或延长原始画面的保留时间；以成片实际播放时长判断可读性，通常留约 2–3 秒，复杂结果适当更久。迅速消失且重要的消息可明确标注“暂停查看”后定格，不能伪装成应用自然持续显示。
- 无信息的误点和重复尝试可以省略；真实功能失败必须保留或在结论中说明。修复前后片段明确标出对应版本或轮次，不能拼成一次连续成功操作。
- 加简短段落标题和操作倍速提示，避免遮住按钮、参数、状态栏或错误。只在有助于辨认结果时做局部放大，不将重绘界面当作录像。
- 加速不代表应用响应速度；不依据剪辑后的等待长度评价性能。原录音随对应画面同步处理速度，或在无须声音证据时去掉原音轨；后配旁白按下节单独对齐，保持正常语速。

## 为成片添加简短旁白

- 默认为验收视频添加中文旁白并保留阶段小标题；用户指定其他语言、声音或无声交付时遵从其要求。已有清楚且对齐的讲解可直接保留，不重复叠加。
- 先检查已有旁白：已包含等待说明时，复用原音频并补齐对应字幕，不重复配音；缺少等待说明时只补缺失的说明。字幕与该句旁白同时出现在对应片段，耗时、估计范围和“等待已剪除”的含义一致；用户明确要求无声时保留完整字幕披露。
- 先确定精剪后的时间线，再围绕每段的“做什么、结果是什么”写简短口语，避免逐字念编号和标题。尺寸与单位读清楚；只讲画面或核验支持的事实，不把预期结果说成已经通过。
- 分句生成语音，按成片中的动作与结果对齐，保留自然停顿。画面操作 2 倍速不代表旁白也加速；句子放不下时先精简表达，必要时多保留原有结果画面，不能切断尾音、叠句或跨到无关步骤。
- 中文旁白默认用 `edge-tts` 的微软晓晓（`zh-CN-XiaoxiaoNeural`），以正常语速分句生成。它调用在线语音服务；仅提交需要配音的文字。其他语言或用户明确指定声音时遵从其要求。
- 晓晓因网络、服务或工具不可用而生成失败时，macOS 使用已安装的 `say -v Tingting`（婷婷）作为 fallback；在交付中说明实际声音及回退原因，不把婷婷版称为晓晓版。两者均不可用时说明配音未完成，保留带标题的视频。可懂、能帮助验收即可，不为反复调音色拖延交付。
- 配音依赖安装在用户全局工具目录，不装进业务项目。优先复用现有 `edge-tts`；需要安装时使用 `uv tool install edge-tts`，默认命令在 `~/.local/bin/edge-tts`，独立环境在 `~/.local/share/uv/tools/edge-tts`。语速按语言、片段长度和试听调整，不固定某个样片的参数。
- 在剪辑映射旁保存旁白文本、成片起止时间、实际音频时长和所用声音，检查每句是否落在对应片段内。原录音有验收价值时保留，旁白避让关键声音；其余情况下可去掉无关原音。合成旁白与录制现场声音应明确区分。

## 验收视频网页的章节导航

- 网页中的视频包含多个验收章节、适合快速跳转时，在播放器附近提供简洁的章节按钮；单一短片无需强行添加导航。
- 按钮文字统一为“时间戳 + 章节名称”，例如“00:00 顶板默认值”“00:42 更新结果”，按时间顺序排列。时间戳取自**最终成片**的章节时间映射，文字与按钮实际跳转位置使用同一时间值，不混用原片时间；重新剪辑后同步更新映射、按钮文字与跳转位置。
- 时间戳使用 `MM:SS`，超过一小时使用 `HH:MM:SS`；若章节落在小数秒，明确显示相应精度并跳到同一时刻，不让显示取整后的时间与实际跳转值不一致。
- 手机上按钮文字应清楚完整、点击区域足够且互不重叠；按钮可换行排列，保持间距，不以缩小字号或截断章节名称来塞进一行。导航不遮挡视频、关键结果或等待披露字幕，也不替代已有旁白说明。
- 用一个多章节示例逐个点击核对“最终成片映射 → 按钮时间戳与名称 → 播放器实际跳转位置及对应章节画面”，并检查手机宽度下的可读性和点击区域；重新剪辑后再次核对，不能只验证按钮文本或点击事件已触发。

## 成片检查与交付

- 优先导出兼容手机的 H.264 MP4，使用 yuv420p 和 faststart。保留足够分辨率让目标控件、文本与鼠标看得清；压缩目标是方便观看，不牺牲判断结果所需的细节。
- 交付网页时，在用户实际使用的浏览器中播放并跳转检查画面和声音；文件可解码或另一浏览器正常不能替代这项检查。播放器黑屏等异常保留实际现象，不把重新打开后恢复写成根因已修复。
- 并行素材按顺序回放时，明确标出相同原始时段；成片先后顺序不能用来判断实际操作是否重叠。
- 检查文件能完整解码、时长和速度合理；抽看开头、各验收项的关键操作与结果、报错、片段衔接和结尾。字幕时间、段落位置和结果停留时间都按成片检查。
- 抽取一个包含明显应用等待的短片段，逐项核对原片／可信记录的耗时、被剪除区间、成片剪点、等待字幕与旁白是否对应；确认字幕可读且不遮挡关键结果，操作和结果均保留。没有此类等待时不为示例虚构应用耗时。剪辑后的流畅表现不能作为应用响应速度的验收结论。
- 检查 Blender/Bonsai 成片中的三维操作与结果均为透视画面；误录正交时，在已有录制授权范围内用透视重录相应证据链，不能仅剪掉必要证据或后期变形冒充透视。仅受托处理已有素材且缺少透视画面时，报告素材不满足要求，不擅自重录或声称符合要求。
- 配音版导出兼容手机的音轨（如 AAC），核对非静音、音量、句间间隔及结尾完整性；能够试听时核对数字、单位发音与画面同步，不能把仅检查音轨参数说成已试听。交付时简短注明合成配音，并提醒开启声音；需要用户判断音色时直接提供可播放样片。
- 用户要求手机观看时，使用 `on-mobile` 技能生成可访问链接；只暴露交付所需媒体，检查 HTTPS、视频类型以及 Range 请求返回 206，便于播放和拖动进度。链接依赖本机或临时服务时说明有效条件，不将本地绝对路径当成手机链接。
- 在当前对话直接交付视频或可观看链接，并简述：成片时长、已验证内容、失败或未覆盖项。只有有助于定位时才加少量时间点；请用户按时间点反馈即可，不让用户重做一长串手工验收。
- 所有约定项目通过时表述为“本轮录屏验收通过”；不声称整个产品永无问题，也不代替用户作最终验收确认。
